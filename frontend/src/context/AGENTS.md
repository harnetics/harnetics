# context/
> L2 | 父级: /frontend/src/AGENTS.md

成员清单
ThemeContext.tsx: 主题 React Context — ThemeProvider 包裹根组件，useTheme() hook 供子组件消费；管理 dark/light 切换、localStorage 持久化、系统偏好跟随。

法则: context/ 只存放 React Context 与 Provider，不包含业务逻辑。

[PROTOCOL]: 变更时更新此头部，然后检查 AGENTS.md
