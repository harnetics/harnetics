# 2026-05-04

## 会话摘要
修复桌面端删除确认依赖浏览器原生 `window.confirm` 的问题。

## 记录
- [修复] 新增 `frontend/src/components/ui/confirm-dialog.tsx` 应用内确认弹窗，替代 WebView 中不可靠的浏览器原生 confirm。
- [修复] `Comparison.tsx` 历史比对记录删除改为应用内确认；`Documents.tsx` 单文档删除与批量删除同步替换，避免同类桌面端失败。
- [验证] `frontend npm run build` 通过；`rg "window.confirm"` 确认业务代码无残留调用。
- [进展] 新增 `HARNETICS_COMPARISON_STEP3_MAX_TOKENS`，后端 Settings、`.env.example`、README 与设置页高级配置已打通，Step3 LLM 批评估不再使用硬编码输出预算。
- [修复] 四步比对 Step4 在模型 JSON 缺少 `summary` 时改用 findings 生成确定性全局结论，避免最终报告出现“未生成全局结论”。
- [验证] `uv run pytest tests/test_app.py tests/test_comparison_4step.py -q` 与 `frontend npm run build` 通过。
- [修复] comparison API 读取/导出历史报告时自动修复已持久化的“全局结论：未生成全局结论”，并将 comparison created_at 出口统一转换为北京时间。
- [修复] 前端新增 `formatBeijingTime()`，Comparison/Draft/Impact 相关页面不再手写 `slice()`，时间展示统一走北京时间格式化。
- [数据] 已修复 `desktop/var/harnetics-graph.db` 中 2 条旧四步比对报告的全局结论占位。
- [验证] 新增 API 回归测试覆盖历史 summary 修复与北京时间转换；`uv run pytest tests/test_app.py tests/test_comparison_4step.py -q`、`frontend npm run build` 通过。
- [诊断] `desktop/data/logs/2026-05-04_10-38-23.log` 显示同一文件三次四步比对 Step1 需求数分别为 70、59、147；第三次因 Step1 JSON 校验失败退回 PDF 页面章节 fallback，根因是 Step1 需求集不稳定。
- [修复] 四步比对 Step1 增加按审查文件内容哈希的进程内需求缓存，同一要求文件复用首次成功需求集；Step1/Step3/Step4 结构化 LLM 调用显式 `temperature=0.0`。
- [防护] 新增确定性编号条款 fallback 但加 200 条上限；真实日志样本会扫出 925 个编号候选，已确认被上限挡住，避免把正文引用编号误当需求。
- [验证] `uv run pytest tests/test_app.py tests/test_comparison_4step.py tests/test_llm_client.py -q` 通过 28 项。
- [进展] 接入 `docs/image/logo.png` 作为桌面应用图标源，生成 `desktop/src-tauri/icons/icon.png`、`icon.ico`、`icon.icns` 并写入 `tauri.conf.json` 的 `bundle.icon`。
- [验证] 校验 Tauri icon 路径均存在，`frontend npm run build` 与 `git diff --check` 通过。
- [修复] Tauri 构建要求 PNG icon 为 RGBA；已将 `desktop/src-tauri/icons/icon.png` 从 RGB 转为 RGBA，并重新派生 `icon.ico` / `icon.icns`。
- [修复] macOS 桌面首次打开需要 Reopen 的根因是窗口先加载固定 8000 端口，sidecar 动态端口尚未健康；Tauri 主窗口改为默认隐藏，后端 60 秒健康检查通过后再 navigate + show。
- [诊断] DMG 中 `.VolumeIcon.icns` 可见的根因是 Tauri 生成脚本只设置 creator，没有设置 filesystem hidden flag；新增 `desktop/scripts/fix-macos-dmg.mjs` 在 `npm run build` 后处理 DMG 并隐藏该文件。
- [验证] `npm run build` in `desktop/` 通过，挂载产物确认 `.VolumeIcon.icns` 带 `hidden` flag；`uv run pytest tests/test_desktop_tauri_config.py tests/test_desktop_runtime.py tests/test_developer_logs.py -q`、`cargo test`、`frontend npm run build`、`git diff --check` 通过。
- [发布] 准备 v0.2.0：统一 `pyproject.toml`、desktop package、Cargo 与 Tauri 版本号，补齐 CHANGELOG，并让 desktop release workflow 走 `npm run build -- --target ...` 以执行 macOS DMG 后处理。
- [验证] `npm run build -- --target aarch64-apple-darwin` 生成 `Harnetics_0.2.0_aarch64.dmg`；挂载确认 `.VolumeIcon.icns` 为 hidden；相关 pytest、cargo test、json 校验与 diff check 通过。

## Retain
- → bank/experience: 桌面 WebView 不应依赖 `window.confirm`/原生浏览器弹窗完成关键破坏性操作；删除确认应使用应用内 modal。
- → bank/experience: LLM 返回结构化 JSON 但缺少可选字段时，应在解析归一化层补确定性 fallback，而不是让展示层暴露“未生成”占位。
- → bank/experience: 修复已持久化的错误展示不能只改生成路径；必须在读取/导出出口加 legacy repair，必要时同步修复本地数据库。
- → bank/experience: 多轮 LLM 工作流的入口集合必须稳定；同一输入文件应缓存或确定化 Step1，否则后续统计波动会被误判为评估质量问题。
- → bank/world: 桌面安装包图标源图位于 `docs/image/logo.png`，Tauri bundle 引用 `desktop/src-tauri/icons/icon.png`、`icon.ico`、`icon.icns`。
- → bank/experience: Tauri DMG 的 `.VolumeIcon.icns` 可能缺少 hidden flag；正式 build 后需挂载后处理或上游修复，否则 Finder 显示隐藏文件时安装窗口会露出内部图标文件。
- → bank/experience: 桌面壳不应让 WebView 在 sidecar 健康前加载固定端口；应隐藏窗口、等待健康检查、导航到实际动态端口后再显示。
- → bank/experience: Tauri `npm run build -- --target ...` 的参数只会传给 `build` 脚本本体；构建后处理应放在 npm `postbuild` lifecycle，不能写成 `build: tauri build && node ...`。
- [修复] desktop release workflow 的 macOS x64 runner 从 `macos-13` 调整为 `macos-15-intel`，避免旧标签在发布时长时间排队导致 asset publish 卡住。
- [处置] 取消了错误的 `v0.2.0` release run，准备在修复后重打 tag 并重新发布。
