# 2026-04-12

## 会话摘要
定位“服务日志仍显示本地 Ollama/gemma4”问题，修复无参 LLM 默认构造与 `.env` 路径解析语义，并把服务端实际生效的模型/基地址暴露到状态端点。

## 记录
- [决策] `HarneticsLLM()` 无参构造不再直接锁死 `gemma4:26b`，而是先读取当前 settings，再用已解析的 base URL 参与模型归一化。
- [决策] `get_settings()` 的 `.env` 解析改为 `HARNETICS_ENV_FILE > cwd/.env > repo-root/.env`，避免从子目录或 reload 进程启动时悄悄掉回默认本地配置。
- [决策] `GET /api/status` / `GET /api/dashboard/stats` 新增 `llm_base_url`、`llm_effective_model`、`llm_effective_base_url`、`embedding_base_url` 与 `config_env_file`，把“服务进程实际看到的路由”直接暴露出来。
- [经验] 只在交互 shell 里打印 `get_settings()` 还不够；真正需要的是服务进程自己公开 effective route，否则 uvicorn 子进程和共享 shell 很容易制造假象。
- [经验] 只要库允许默认构造，默认值就会变成隐蔽分叉点；默认构造应该继承当前运行时配置，而不是编码阶段的历史默认模型。
- [进展] 为 `test_llm_client.py` 增加默认构造继承远端网关配置的回归；为 `test_app.py` 增加 repo-root `.env` 回退回归；为 `test_e2e_mvp_scenario.py` 增加 status effective route 字段断言。

## Retain
- → MEMORY: 运行时排查“为什么还在走本地模型”时，优先看 `/api/status` 返回的 `llm_effective_model`、`llm_effective_base_url` 和 `config_env_file`，不要只看 shell 里的 `get_settings()`。
- → bank/experience: 无参 LLM 默认构造必须继承当前 settings；否则一旦漏注入，就会静默回退到历史默认模型，制造非常难查的伪本地路由。