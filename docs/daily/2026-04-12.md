# 2026-04-12

## 会话摘要
完成 `005-openai-compatible-llm-client` 主实现：远端 completion/embedding 收敛到 OpenAI-compatible SDK，补齐 env-routing、状态诊断、impact section fallback 与回归验证闭环。

## 记录
- [决策] 远端 LLM completion 改为 OpenAI SDK `chat.completions.create()`；请求体直接发送原始模型名，normalized provider/model 只保留给诊断层与落库元数据。
- [决策] 云端 embedding 同步切换到 OpenAI-compatible `embeddings.create()`，这样 impact 的 ai_vector 路径与 completion 路径共享同一组 base/model 语义。
- [决策] `GET /api/status` / `GET /api/dashboard/stats` 共享短 TTL 的 LLM 探测缓存，避免同一时刻重复打远端网关造成 dashboard 别名返回不一致。
- [决策] `DraftGenerator` 对 `generated_by` 做字符串化收口；mock 或非字符串模型标识一律回落到类名，不再把测试替身对象直接写进 SQLite。
- [决策] `/api/draft/generate`、`DraftGenerator` 与 OpenAI-compatible chat helper 的关键阶段统一输出到 `uvicorn.error`，确保 live 500 时控制台能直接看到入口参数、effective route、LLM 调用阶段与落库阶段。
- [经验] AIHubMix 这类 OpenAI-compatible 网关可能拒绝同时发送 `temperature` 与 `top_p`；兼容性更好的默认做法是只保留一个采样参数。
- [经验] `ai_vector` 路径如果没找出 section hits，必须回落到现有 heuristic/section-aware 边逻辑；否则图上明明有精确边，结果却返回空 `affected_sections`。
- [经验] `load_dotenv(..., override=False)` 会让已注入进程环境的旧值压过测试临时 `.env`；env-routing 回归必须先清空 `HARNETICS_*` / `OPENAI_*` 再显式指定 `HARNETICS_ENV_FILE`。
- [经验] 单纯在业务模块里用 `logging.getLogger(__name__)` 不一定能进 Uvicorn 控制台；如果目标是 live 排障，最稳妥的是直接打到 `uvicorn.error` 或显式接入它的 handler。
- [进展] 目标回归 `.venv/bin/python -m pytest tests/test_llm_client.py tests/test_graph_store.py tests/test_env_routing.py tests/test_app.py tests/test_e2e_mvp_scenario.py -q` 全部通过：`42 passed, 16 warnings in 17.91s`。
- [进展] 追加回归 `.venv/bin/python -m pytest tests/test_app.py tests/test_e2e_mvp_scenario.py -q` 通过：`22 passed, 15 warnings in 17.61s`；并在临时 `:8023` 实例上实测看到 `draft.generate.start → llm.chat.* → draft.generator.persisted → draft.generate.success` 全链路日志。

## Retain
- → MEMORY: 运行时排查“为什么还在走本地模型”时，优先看 `/api/status` 返回的 `llm_effective_model`、`llm_effective_base_url`、`embedding_base_url` 和 `config_env_file`，不要只看 shell 里的 `get_settings()`。
- → bank/experience: 对 OpenAI-compatible 网关，默认请求参数要走最保守兼容面；如果模型未声明支持，就不要同时发送 `temperature` 和 `top_p`。
- → bank/experience: dashboard/status 这类别名诊断端点应该共享短期 probe cache；连续独立探测会把外部网关抖动直接放大成前端不一致。