<!--
[INPUT]: 依赖 docs/product-specs/mvp-prd.md 中的交互形态假设与当前仓库现实
[OUTPUT]: 对外提供前端范围、当前状态与后续落位原则
[POS]: docs/ 的界面与交互说明页，服务未来 Web 实现
[PROTOCOL]: 变更时更新此头部，然后检查 AGENTS.md
-->

# Frontend

当前仓库还没有前端代码，但产品交付形态已经明确：本地部署的 Web 应用。

已知前端假设：
- 入口形态：浏览器访问的本地应用
- 候选技术：FastAPI 服务页面 + Jinja2/HTMX 交互
- 主要界面：文档导入、文档库浏览、草稿生成、影响分析、图谱视图、状态仪表盘

落位原则：
- 真正出现 UI 代码时，再新增 `app/`、`web/` 或 `src/` 目录
- 交互需求先以规格沉淀在 `product-specs/`，不要把半成品线框图塞进这里
