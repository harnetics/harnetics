# harnetics/llm/
> L2 | 父级: src/harnetics/AGENTS.md

成员清单
__init__.py: 包入口，导出 LocalLlmClient（向后兼容旧 llm.py 路径）。
client.py: OpenAI-compatible HTTP 客户端，最小 chat completions 调用。

[PROTOCOL]: 变更时更新此头部，然后检查 AGENTS.md
